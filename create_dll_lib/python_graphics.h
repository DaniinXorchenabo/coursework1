#pragma once

float compute(float first, float second);

int my_python_print();

int create_obj(int);

int print_class(int);

/*
css в отдельном файле
внутри папки с проектом создавать тематические папки
 папка pichers
 папка css
 должны сделать 3 разных типа стилей (базовых) (это те, названия которых не создаёт пользователь)
 организовать минимум 3 стиля по ссылке (по id)
 псевдоклассы (минимум 3)










*/